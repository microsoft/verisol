contract Test
{
    function foo (bytes b) public
        post (ret == keccak256(b))
        returns (bytes32 ret)
    {
        ret = keccak256(b);
        return ret; 
    }

    function bar (bytes b) public
    {
        bytes32 local_var = sha256 (b);
        return;
    }
}